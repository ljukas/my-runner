# Gradle intermediates. Kept here rather than in the root .gitignore because the root file is an
# iOS fingerprint source (`bareGitIgnore`) and a new rule there would cost a release its OTA
# eligibility; nothing under modules/ is in the iOS source set.
/build/
